{
  "name": "dev",
  "version": "0.0.1",
  "private": true,
  "description": "Tools and samples for hello-lights.",
  "scripts": {
    "preinstall": "cd electron-device && npm install && cd ../commander-cli && npm install && cd ../lights-repl && npm install && cd ../lights-server && npm install && cd ../",
    "postinstall": "npm link ../ && npm link ./electron-device && npm link ./commander-cli"
  },
  "author": "Rodrigo Jordao <rodrigo.jordao@gmail.com> (https://github.com/jordao76)",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1"
  }
}
